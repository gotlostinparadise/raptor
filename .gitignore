# macOS
.DS_Store

# Python
*.pyc
__pycache__/
.venv/
.pytest_cache/
.test_durations

# RAPTOR startup
.startup-output

# RAPTOR outputs
out/
.out/

# Local
.env*
.claude/*.local.json
.claude/raptor.env

# Compiled test fixtures (rebuilt on demand)
core/analysis/tests/fixtures/*.o
core/analysis/tests/fixtures/demo

# Runtime artifacts
codeql_dbs/
.github/codeql/codeql-scoped-config.yml
engine/semgrep/rules/registry-cache/c.p.*.json

# User-local tuning (regenerate with: raptor tune default)
tuning.json

# SAGE (opt-in; generated by libexec/raptor-sage-setup)
.mcp.json
.sage/
*.sage.key
*.gcov

# OmniRoute gateway config — holds a live API key. Track .omni.sample instead.
.omni

# Censys API credentials — holds a live API key.
.censys
